const assert = require("assert");

describe("Array Test", function() {
    describe("indexOf() method", function() {
        console.log(bundle);
        it("There is no value, return -1", function() {
            assert.equal(-1, [1,2,3].indexOf(5));
        }) 
    })
})

export class MemberModel {
    
    constructor(id, pwd, name, tel) {
        this.userId = id;
        this.userPassword = pwd; 
        this.userName = name;
        this.userMemberTel = tel;
    }
    
}